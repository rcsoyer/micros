/**
 * Feign clients for the microservice webapp.
 * <p> It can have several feign clients for the same microservice in which each of clients have methods to interact
 * with one of the microservice endpoints
 */
package org.acme.micros.cmd.client.webapp;